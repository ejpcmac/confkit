# Adaptation à BÉPO

# hjkl → ctsr
unmap h
unmap j
unmap k
unmap l
map t scroll down
map s scroll up
map c scroll left
map r scroll right

# JK → alt + ts (et TS pour <C-d> / <C-u>)
unmap J
unmap K
map <A-t> navigate next
map <A-s> navigate previous
map T scroll half-down
map S scroll half-up

map <C-l> scroll top

# r → p
map p rotate rotate-cw

# R → u
unmap R
map u reload

# b ↔ a car le a est plus accessible
map b adjust_window best-fit
map a adjust_window width

# f → e car plus accessible
unmap f
map e follow

# Mode Index
map [index] t navigate_index down
map [index] s navigate_index up
map [index] r navigate_index expand
map [index] c navigate_index collapse

map [index] R navigate_index expand-all
map [index] C navigate_index collapse-all